enum ButtonType {
  hover,
  normal,
  ative,
  large
}

enum CardType{
  subtitle,
  normal,
}

enum ErrorRequest {
  internet,
  timeOut,
  serverError,
}

enum Periodo { day, week, month, year, MAX }